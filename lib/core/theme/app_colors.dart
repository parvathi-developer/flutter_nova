import 'package:flutter/material.dart';

class AppColors {
  // Brand colors (same for both themes)
  static const Color novaPrimary = Color(0xFF6C63FF);
  static const Color novaSecondary = Color(0xFF00E5FF);

  // ---------- DARK ----------
  static const Color darkBackground = Color(0xFF0B0E1C);
  static const Color darkSurface = Color(0xFF151A30);
  static const Color darkTextPrimary = Colors.white;
  static const Color darkTextSecondary = Color(0xFFB0B3C6);

  // ---------- LIGHT ----------
  static const Color lightBackground = Color(0xFFF8F9FC);
  static const Color lightSurface = Colors.white;
  static const Color lightTextPrimary = Color(0xFF1A1C2E);
  static const Color lightTextSecondary = Color(0xFF5F627D);
}
